# Copy to .env and set real values. Do not commit .env.

# Flask
APP_ENV=development
PORT=5000

# Required in production — use long random strings (e.g. openssl rand -hex 32)
SECRET_KEY=change-me-to-a-long-random-secret
JWT_SECRET_KEY=change-me-to-another-long-random-secret

# Optional JWT settings
# JWT_ALGORITHM=HS256
# JWT_EXPIRES_SECONDS=3600

# Database (dev default is SQLite if unset in development)
# DATABASE_URL=sqlite:///pos_system.db
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/pos_system
